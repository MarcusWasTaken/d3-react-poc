import styled from 'styled-components'

const Legend = styled.legend`
  background: #0086bf;
  color: #fff;
  margin-left: 16px;
  padding: 4px 12px;

  @media print {
    display: none;
  }
`

export default Legend
